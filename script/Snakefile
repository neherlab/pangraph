from snakemake.utils import min_version
min_version("6.0")

module accuracy_mod:
    snakefile: "snakefile_accuracy"
    config: config

module panx_benchmark_mod:
    snakefile: "snakefile_panx"
    config: config

use rule * from accuracy_mod as acm_*

use rule * from panx_benchmark_mod as pxm_*